# ios/fastlane/Matchfile
git_url(ENV["MATCH_GIT_URL"])
storage_mode("git")
type("appstore")            # default type; lanes override for adhoc/development
app_identifier(["com.vibetrack.app"])
username(ENV["APPLE_ID"])
